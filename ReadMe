to run the App 

Ein lauffägie Version des Projects wurde in einem Docker Container bereitgestellt um das
programm zu auszuführen sollen Sie das Docker ausführen und das Project_ Directory in einem
Terminal öffen und ein Image für das project „docker build -t web_crawler:latest . “ und dann
ein Container für den Server laufen lassen durch „docker run -p 5000:5000
web_crawler:latest“